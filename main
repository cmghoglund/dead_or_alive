# Program that calculates how many days someone has been dead or alive based on their date of death (dod) or date of birth (dob)

# TODO Add input validation and error handling
# TODO Add functionality to handle also dead users

import datetime

def calculate_age_in_days(year, month, day):
    # Get today's date
    today = datetime.date.today()

    # Create a date object from the user's input
    dob = datetime.date(year, month, day)

    # Calculate the number of days between the two dates
    age_in_days = (today - dob).days

    return age_in_days

# Get the user's date of birth
year, month, day = map(int, input("Enter your date of birth (YYYY-MM-DD): ").split('-'))

# Calculate the user's age in days
age_in_days = calculate_age_in_days(year, month, day)

# Print the result
print(f"\nYou have lived for {age_in_days} days.\n")
